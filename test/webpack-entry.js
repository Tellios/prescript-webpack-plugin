// This is the entry file for the webpack config

const scriptGeneratedCode = require('./generatedCode');

module.exports = () => {
    console.log('some dummy code pew pew');
    scriptGeneratedCode();
}
